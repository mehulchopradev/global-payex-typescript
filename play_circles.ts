import { areaCircle as area, perimeterCircle } from "./lib/geometry";


function areaCircle(r: number) {
  console.log('Dummy area circle method')
}

const radius = 6;
console.log(area(radius));
console.log(perimeterCircle(radius));
